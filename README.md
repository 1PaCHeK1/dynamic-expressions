Dynamic-expressions are a library for building, execution and serialization of any expressions, see [documentation](https://notypecheck.github.io/aioinject/)

## Installation
Install using pip `pip install dynamic-expressions`
